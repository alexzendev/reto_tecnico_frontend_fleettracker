export const API_ENDPOINTS = {
    VEHICLES: "/vehicles",
} as const

export const QUERY_CACHE_KEYS = {
  VEHICLE: "vehicle",
} as const;

export const PAGINATION = {
  DEFAULT_PAGE: 1,
  DEFAULT_PAGE_SIZE: 10,
} as const;
